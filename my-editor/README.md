# My Editor ユーザーマニュアル (V3)

「My Editor」は、ブラウザ上で動作する高性能なVSCode風のWebテキストエディタです。インストールの必要がなく、社内のセキュリティ制限が厳しい環境でもブラウザから直接ローカルファイルの読み込み、編集、上書き保存、複数ファイルの管理が可能です。

---

## 1. アプリケーションの概要と特徴

本エディタは、Microsoftが開発しVSCodeの中核として使用されている「Monaco Editor」をベースに構築されています。そのため、VSCodeとほぼ同等の操作感と強力な開発サポート機能を備えています。

### 主な特徴
- **インストールの必要なし**: ブラウザ環境（HTTPS）があればどこでも動作します。
- **ローカルファイルへの直接上書き保存**: 編集した内容は、毎回ダウンロードすることなくPC内の元のファイルへ直接保存されます。
- **マルチ言語ハイライト**: PowerShell、JSON、JavaScript、Python、そして **Markdown (.md)** など、多様な言語のシンタックスハイライトに対応しています。
- **ドラッグ＆ドロップ対応**: PC上のファイルを画面内に落とすだけで、一括で複数ファイルを開くことができます。
- **快適な作業空間**: 目の負担を軽減するダークテーマ、アプリタイトルの常時表示、簡単なズーム調整機能、そして画面上でサッと確認できるショートカットヘルプ（iボタン）を備えています。

---

## 2. 導入方法：GitHubでのフォルダ格納手順

GitHub Pages上で他のツールと混ざらないように、`my-editor/` という専用フォルダに格納して公開する手順です。

1. **GitHubのリポジトリ画面を開く**
   - ご自身のリポジトリ（`zumi1103.github.io`）のトップ画面を開きます。
2. **ファイルをフォルダへ移動する（裏ワザ手順）**
   - リストから **`index.html`** をクリックして開きます。
   - 右上にある **鉛筆マーク（Edit this file）** をクリックします。
   - 画面上部のファイル名入力ボックス（`index.html` と書かれている場所）をクリックし、頭に以下のように付け足します。
     👉 **`my-editor/index.html`**
     *(※ `/` を入力した瞬間に自動的にフォルダの階層構造に切り替わります)*
   - 右上の緑色の **「Commit changes...」** ボタンを押し、確認ポップアップでもう一度「Commit changes」を押して保存します。
3. **残りのファイルも同様に移動する**
   - **`style.css`** と **`app.js`** についても、全く同じ手順（1〜2）を行います。
     👉 `my-editor/style.css`
     👉 `my-editor/app.js`
4. **完了の確認**
   - リポジトリのトップ画面に戻ると、新たに `my-editor` というフォルダが作られ、その中に3つのファイルが綺麗に格納されていることが確認できます。

---

## 3. 画面構成とUI

画面は大きく分けて以下の3つのエリアで構成されています。

1. **ツールバー（上部）**
   - **「My Editor」ロゴ**: アプリケーションのタイトル。左端に常時表示されます。
   - **「ファイルを開く」ボタン**: ローカルファイルを選択するためのボタン。
   - **タブコンテナ**: 開いているファイルのタブが横に並びます。アクティブなタブには青い上線が表示されます。
   - **ズームコントロール**: 画面の文字サイズを調整する「＋」「－」「R（リセット）」ボタン。
   - **「i」ヘルプボタン**: クリックするとショートカットキー一覧のポップアップが表示されます。
2. **メインエリア（中央）**
   - **エディタ画面**: ファイルが開かれている時にコードやテキストを表示・編集するエリア。
   - **空画面（案内）**: ファイルが一つも開かれていない時に表示される、ダークな背景のドラッグ＆ドロップエリア。
3. **通知領域（右下）**
   - **トースト通知**: ファイル保存時などに「保存しました！」とフワッと青いポップアップが出現します。

---

## 4. 基本的な使い方

### 4.1 アプリへのアクセス方法
GitHub Pagesで公開されている以下のURLにブラウザ（ChromeまたはEdgeを推奨）でアクセスします。
* **`https://zumi1103.github.io/my-editor/`**
* ※セキュリティ制限（File System Access APIの仕様）により、ローカルファイルを直接ダブルクリックして開いた状態（`file:///...`）では動作しません。必ず上記のHTTPS環境からアクセスしてください。

### 4.2 ファイルを開く方法
ファイルを開く方法は2通りあります。どちらの方法でも**同時に複数のファイルを選択して一括で開くことが可能**です。

#### 方法A：ボタンから開く
1. ツールバーの**「ファイルを開く」**ボタンをクリックします。
2. ファイル選択ダイアログが表示されます。
3. 開きたいファイルを選択し、「開く」をクリックします。
   - *複数開く場合*: `Ctrl`キーまたは`Shift`キーを押しながらファイルをクリックすることで、複数のファイルを同時に選択できます。

#### 方法B：ドラッグ＆ドロップで開く
1. PC上のフォルダやデスクトップから、開きたいファイルをマウスで掴みます（複数選択も可）。
2. ブラウザの画面（どこでも可）へ移動させ、マウスを離してドロップします。
3. 選択したすべてのファイルが一瞬でタブとして展開されます。

> **⚠️ 初回ファイル読み込み時の注意**
> ブラウザから「ファイルを編集する権限を許可しますか？」という旨の確認ポップアップ（セキュリティ警告）が表示された場合は、必ず**「許可」**または**「変更を保存する」**を選択してください。拒否すると上書き保存が機能しなくなります。

### 4.3 コードの編集と対応言語
ファイルを開くと、自動的に拡張子を判別し、VSCodeと同様にキーワードや変数に色がつくシンタックスハイライトが適用されます。
特に、**Markdown (`.md`) ファイル** にも完全対応しており、見出しやリンク、太字などがエディタ上で綺麗に色分けされます。
また、文字を入力するとメソッドやタグの候補が自動的に表示される「入力補完（オートコンプリート）」機能も動作します。

### 4.4 タブの切り替えと閉じる方法
- **切り替え**: ツールバーに並んでいるタブをマウスクリックすると、一瞬でそのファイルのエディタ画面に切り替わります。
- **閉じる（マウス）**: タブの右側にある「×」ボタンをクリックすると、そのタブが閉じます。
- **閉じる（キーボード）**: 後述のショートカットを使用することで、マウスを操作せずに現在選択中のタブをサクサク閉じることができます。
- すべてのタブを閉じると、画面は自動的にダークな「空画面（案内）」に戻ります。

### 4.5 ファイルの上書き保存
編集が終わったら、保存の操作を行うことで、PC内の元のファイルに内容が直接上書きされます。
保存が成功すると、画面右下に青いトースト通知で**「保存しました！」**と表示され、3秒後に自動的に消えます。

### 4.6 ズーム（文字サイズ）の調整
文字が小さくて見づらい場合や、全体を俯瞰したい場合は、文字の大きさを変更できます。
- ツールバー右側の**「＋」**ボタンで拡大します。
- ツールバー右側の**「－」**ボタンで縮小します。
- ツールバー右側の**「R」**ボタンを押すと、標準の文字サイズに一発でリセットされます。

---

## 5. キーボードショートカット一覧

| 機能 | ショートカットキー (Windows) | ショートカットキー (Mac) | 備考 |
| :--- | :--- | :--- | :--- |
| **ファイル上書き保存** | `Ctrl` + `S` | `Cmd` + `S` | ブラウザ標準の保存を無効化し、直接上書きします。成功すると右下にトーストが出ます。 |
| **アクティブなタブを閉じる** | `Alt` + `W` | `Option` + `W` | ※`Ctrl+W`はブラウザの仕様でブロックされるため、代わりにこのキーを割り当てています。連打で連続して閉じられます。 |
| **画面のズームイン（拡大）** | `Ctrl` + `マウスホイール上` <br> または `Ctrl` + `+` / `=` | `Cmd` + `マウスホイール上` <br> または `Cmd` + `+` / `=` | フォントを大きくします。 |
| **画面のズームアウト（縮小）** | `Ctrl` + `マウスホイール下` <br> または `Ctrl` + `-` | `Cmd` + `マウスホイール下` <br> または `Cmd` + `-` | 画面を縮小します。 |
| **ズームリセット** | `Ctrl` + `0` | `Cmd` + `0` | 一瞬で標準サイズに戻します。 |

---

## 6. サポートされているプログラミング言語の一覧

- **Markdown** (`.md`) 🌟
- **PowerShell** (`.ps1`)
- **JSON** (`.json`)
- **JavaScript** (`.js`)
- **TypeScript** (`.ts`)
- **HTML** (`.html`)
- **CSS** (`.css`)
- **Python** (`.py`)
- **XML** (`.xml`)
- **SQL** (`.sql`)
- **Java** (`.java`)
- **C#** (`.cs`)
- **C言語 / C++** (`.c`, `.cpp`)
- **シェルスクリプト / バッチ** (`.sh`, `.bat`)

---

## 7. トラブルシューティング

#### Q. 「ファイルを開く」ボタンを押しても何も起きない、または保存時にエラーが出る
- **原因1**: HTTPS環境（`https://...`）ではなく、ローカルファイル（`file:///...`）としてブラウザで直接開いていませんか？GitHub PagesのURLからアクセスし直してください。
- **原因2**: 権限確認ポップアップを拒否していませんか？ページを一度リロードし、開き直して権限を「許可」してください。
- **原因3**: ブラウザのバージョンが古い、または未対応です。最新の **Google Chrome** または **Microsoft Edge** をご使用ください。

---

## 8. 開発資料

本エディタの開発プロセスや実装の詳細な経緯については、以下のチャットログをご参照ください。
- [My Editor 開発チャットログ (Gemini)](https://gemini.google.com/app/12ba6873b1b79e68?utm_source=app_launcher&utm_medium=owned&utm_campaign=base_all)
